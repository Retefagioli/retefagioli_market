import 'package:flutter/material.dart';
class CircleIconCustom extends StatelessWidget {
  const CircleIconCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}